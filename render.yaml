services:
  # Species Tutorial Frontend
  - type: web
    name: species-tutorial
    env: static
    buildCommand: npm ci && npm run build
    staticPublishPath: ./build
    pullRequestPreviewsEnabled: true
    rootDir: species-tutorial
    nodeVersion: 18